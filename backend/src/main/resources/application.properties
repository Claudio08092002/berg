# Server Configuration
server.port=8080

# Application Name
spring.application.name=berg-backend

# CORS Configuration (allows Angular frontend to connect)
# For development - allow localhost:4200 (Angular default port)

# Logging
logging.level.root=INFO
logging.level.com.berg=DEBUG
